// A function for strict equality check
export function addNumbers(a: number, b: number): number {
  return a + b;
}

// An example object to check deep equality
export const exampleObject = { a: 1, b: 2 };
